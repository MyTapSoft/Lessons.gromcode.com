CREATE TABLE FILES(
id NUMBER PRIMARY KEY,
NAME NVARCHAR2(64) NOT NULL,
FORMAT NVARCHAR2(64) NOT NULL,
FILE_SIZE NUMBER NOT NULL,
STORAGE_ID NUMBER,
CONSTRAINT STORAGE_FK FOREIGN KEY (Storage_id) REFERENCES Storage(STORAGE_ID)
);